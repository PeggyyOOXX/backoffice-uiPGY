# Backoffice UI

101 BEST 後台 UI／UX 互動原型，包含會員管理、共用搜尋篩選、列表工具列、分頁、樣式規範與響應式側欄。

## 本機啟動

```bash
npm install
npm run dev
```

開啟終端顯示的本機網址即可查看。

## 建置

```bash
npm run build
npm run preview
```

此 Repository 為獨立設計原型，不包含 101 BEST 正式專案原始碼。
